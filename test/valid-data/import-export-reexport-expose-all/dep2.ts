export interface Dep2_Reexported {
    nested?: boolean;
}

export interface Dep2_NotReexported {
    nested?: boolean;
}

export interface Dep2_DirectlyReexported {
    nested?: boolean;
}

export interface Dep2_AsTypeReexported {
    nested?: boolean;
}

export interface Dep2_AsTypeNotReexported {
    nested?: boolean;
}

export interface Dep2_AsTypeDirectlyReexported {
    nested?: boolean;
}
