export interface Reexported {
    nested?: boolean;
}

export interface NotReexported {
    nested?: boolean;
}

export interface DirectlyReexported {
    nested?: boolean;
}

export interface AsTypeReexported {
    nested?: boolean;
}

export interface AsTypeNotReexported {
    nested?: boolean;
}

export interface AsTypeDirectlyReexported {
    nested?: boolean;
}
